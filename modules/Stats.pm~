package Stats;

use Moose;

has 'n_contigs'    => (is => 'rw', isa => 'Int', required =>1);
has 'total_length' => (is => 'rw', isa => 'Int', required =>1);
has 'n_seqs'       => (is => 'rw', isa => 'Int', required =>1);
has 'n_tags'       => (is => 'rw', isa => 'Int', required =>1);

1;
